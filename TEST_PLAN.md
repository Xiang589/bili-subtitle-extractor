# TEST_PLAN

以下为 v0.1 手动测试计划。测试前请安装 `bili-subtitle-extractor.user.js`，并确认浏览器已启用 Tampermonkey。

## 1. 单 P 有字幕视频

1. 打开一个单 P 且已有字幕的 Bilibili 视频。
2. 点击右下角“提取字幕”。
3. 导出格式输入 `md` 或直接回车。
4. 预期：浏览器下载 Markdown 文件，文件名包含视频标题、BV 号和 `P1`。
5. 预期：Markdown 中包含标题、来源、BV 号、CID、分 P、原始链接、字幕语言和字幕正文。

## 2. 多 P 视频

1. 打开一个多 P 视频的 `p=2` 页面。
2. 点击右下角“提取字幕”。
3. 导出 Markdown。
4. 预期：脚本读取 P2 对应的 CID。
5. 预期：下载文件名包含 `P2`。

## 3. 无字幕视频

1. 打开一个没有字幕轨的视频。
2. 点击右下角“提取字幕”。
3. 预期：弹窗提示“当前视频没有字幕轨”。
4. 预期：不生成空文件。

## 4. 多字幕轨

1. 打开一个有多条字幕轨的视频。
2. 点击右下角“提取字幕”。
3. 预期：脚本优先推荐中文简体、中文或 AI 中文。
4. 预期：也可以在提示框中输入序号选择字幕轨。

## 5. 需登录字幕

1. 在未登录或权限不足的情况下打开一个字幕需要登录权限的视频。
2. 点击右下角“提取字幕”。
3. 预期：弹窗提示需要登录。
4. 预期：脚本不崩溃，不生成空文件。

## 6. SRT 导出

1. 打开一个已有字幕的视频。
2. 点击右下角“提取字幕”。
3. 导出格式输入 `srt`。
4. 预期：每条字幕序号从 1 开始递增。
5. 预期：时间格式为 `00:00:00,000 --> 00:00:03,500`。
6. 预期：条目之间有空行。

## 7. JSON 导出

1. 打开一个已有字幕的视频。
2. 点击右下角“提取字幕”。
3. 导出格式输入 `json`。
4. 预期：下载 JSON 文件可被 `JSON.parse` 解析。
5. 预期：JSON 包含 `title/source/url/bvid/cid/page/track/body` 字段。
